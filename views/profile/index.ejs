<h1><%= currentUser.firstname %>'s Profile Page</h1>

<div class="container">
  <div class="left">
    <img src="<%= currentUser.profile %>" alt="user profile pic">
    <p><strong>Name:</strong> <%= currentUser.firstname %> <%= currentUser.lastname %></p>
    <p><strong>Admin?</strong> <%= currentUser.admin ? 'Yes' : 'No' %></p>
  </div>
  <div class="right">
    <p>
      <%= currentUser.firstname %> was born on <%= moment.utc(currentUser.birthday).format('MMMM Do YYYY') %> and uses the display name <%= currentUser.username %>. You can email <%= currentUser.firstname %> at <%= currentUser.email %>!
    </p>
    <hr>
    <h2><%= currentUser.firstname %>'s Bio</h2>
    <p><%= currentUser.bio %></p>
  </div>
</div>
